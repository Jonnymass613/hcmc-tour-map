# Tour Site (static)

Open `tour-site/index.html` in a browser.

Notes:
- The map uses OpenStreetMap tiles (internet required).
- Routes are drawn using the public OSRM demo router; if it’s unavailable, the app draws straight lines.
- Prices are budgeting estimates; verify live ticket prices and hotel rates.
